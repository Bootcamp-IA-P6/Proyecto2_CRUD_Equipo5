from django.apps import AppConfig


class RentingConfig(AppConfig):
    """Configuration for the Renting application"""
    name = "renting"
    verbose_name = "Renting"  # Optional: better display name in admin
